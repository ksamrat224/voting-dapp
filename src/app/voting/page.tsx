'use client'
import VotingFeature from '@/features/voting/voting-feature'

// Disable static generation for this page
export const dynamic = 'force-dynamic'

export default function Page() {
  return <VotingFeature />
}
